{EventEmitter} = require 'events'

module.exports = class Property extends EventEmitter
  
  # constructor: (args) ->
    
  #   # super
    
  #   return unless args?
    
  #   for key, value of args
  #     @[key] = value
    
  #   @hook ?= (object, args...) ->
  
  # augment: (instance) ->
    
  #   